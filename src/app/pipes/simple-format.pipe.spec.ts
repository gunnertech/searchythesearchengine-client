import { SimpleFormatPipe } from './simple-format.pipe';

describe('SimpleFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SimpleFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
